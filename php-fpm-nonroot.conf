[www]
; Align workers with bind-mount ownership prepared by entrypoint.sh.
user = app
group = app

; Redirect worker output to FPM error_log (which goes to container logs).
catch_workers_output = yes

; Preserve environment variables for worker processes.
clear_env = no

